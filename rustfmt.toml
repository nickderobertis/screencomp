# Stable rustfmt options only — keep formatting reproducible on the pinned
# stable toolchain (no nightly-only settings).
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
