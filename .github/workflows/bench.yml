name: Performance

# Informational performance suite. On every PR it runs the Criterion in-process
# benchmarks and the hyperfine end-to-end CLI timings, then posts the numbers as
# a sticky PR comment and a job summary. It is deliberately NOT a quality gate:
# benchmark timings are noisy on shared CI runners, so this job reports rather
# than blocks — keep it out of branch-protection's required checks. (The hard,
# deterministic gate is ci.yml / `just full-check`.)

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Read the code; write PR comments. The comment step is additionally guarded to
# same-repo PRs below, since fork PRs only ever get a read-only token.
permissions:
  contents: read
  pull-requests: write

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  bench:
    name: benchmarks
    # A single, consistent runner: timings are only comparable on the same
    # hardware, so this never matrixes across OSes the way ci.yml does.
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so the PR base commit is available for the comparison.
          fetch-depth: 0

      - name: Provision pinned toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just,hyperfine,critcmp

      # Fresh baselines every run: rust-cache may carry a stale target/criterion
      # across runs, which would silently corrupt the base-vs-PR comparison.
      - name: Clear stale Criterion baselines
        run: rm -rf target/criterion

      - name: Benchmark this revision (Criterion)
        run: cargo bench --locked --bench commands -- --measurement-time 3 --save-baseline pr

      - name: Benchmark this revision (hyperfine CLI)
        run: just bench-cli

      # Best effort: the PR base may predate the bench (e.g. the PR that
      # introduces it), so a failure here is expected and non-fatal — the job
      # still reports absolute numbers, just without a regression delta.
      - name: Compare against PR base (Criterion)
        id: basecmp
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          set -euo pipefail
          base='${{ github.event.pull_request.base.sha }}'
          git checkout -q "$base"
          cargo bench --locked --bench commands -- --measurement-time 3 --save-baseline base
          git checkout -q -
          critcmp base pr | tee target/bench/critcmp.txt

      - name: Assemble report
        run: |
          set -euo pipefail
          report=target/bench/report.md
          {
            echo "## ⏱️ Performance report"
            echo
            echo "Commit \`${GITHUB_SHA::8}\` on \`ubuntu-latest\`. Timings are"
            echo "informational — shared-runner noise means treat small deltas with caution."
            echo
            echo "### End-to-end CLI latency (hyperfine)"
            echo
            cat target/bench/results.md
            echo
            echo "### In-process micro-benchmarks vs base (Criterion + critcmp)"
            echo
            if [ -s target/bench/critcmp.txt ]; then
              echo '```'
              cat target/bench/critcmp.txt
              echo '```'
            else
              echo "_No base comparison: the bench is not present on the base ref"
              echo "yet (expected on the PR that introduces it). Future PRs will"
              echo "show the regression delta here._"
            fi
          } > "$report"
          cat "$report" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload raw results
        uses: actions/upload-artifact@v4
        with:
          name: bench-results
          path: |
            target/bench/results.json
            target/bench/results.md
            target/bench/critcmp.txt
          if-no-files-found: warn

      # Same-repo PRs only: fork PRs carry a read-only token and cannot comment,
      # so they fall back to the job summary + artifact above.
      - name: Post sticky PR comment
        if: >-
          github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- screencomp-perf -->';
            const body = `${marker}\n${fs.readFileSync('target/bench/report.md', 'utf8')}`;
            const { owner, repo } = context.repo;
            const issue_number = context.payload.pull_request.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner, repo, issue_number,
            });
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }
